spring:
    application:
        name: MySpringBootApp  # 애플리케이션 이름 설정

    config:
        activate:
            on-profile: local

    datasource:
        url: jdbc:mariadb://svc.sel4.cloudtype.app:32389/invu
        username: devs32
        password: 1989DEVS#@
        driver-class-name: org.mariadb.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: none
        properties:
            hibernate:
                format_sql: true
                show_sql: true

server:
    port: 8080  # 서버 포트 설정
